spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/dotger?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8&allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: 1234
  jpa:
    open-in-view: false
    generate-ddl: true
    show-sql: true
    hibernate:
    ddl-auto: update
server:
  port: 8888
logging:
  level:
    root: warn
    com.example.hibia : DEBUG
file:
  upload:
    location: /Users/82104/Desktop
